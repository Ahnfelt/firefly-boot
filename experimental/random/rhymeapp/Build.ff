import BuildSystem from ff:core

main(system: BuildSystem) {
    Log.debug("Compiling...")
    system.compile("Main.ff", "browser")
    system.compile("Main.ff", "node")
    system.bundle()
    system.executable(assets = [
        "../../../index.html"
        "../browser/**/*"
    ])
    Log.debug("Done.")
}
