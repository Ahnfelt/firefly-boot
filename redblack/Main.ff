import RbMap

main(system: System): Unit {

    mutable map : RbMap[Int, String] = RbLeaf
    // TODO: Why does the parser not like [ on the beginning of a line?
    []
    let pairs = [
        Pair(3, "Three"),
        Pair(2, "Two"),
        Pair(1, "One"),
    ]
    pairs.each {| Pair(k, v) =>
        map = map.add(k, v)
    }
    log.debug(map)

    map = map.remove(2)
    log.debug(map)

}
