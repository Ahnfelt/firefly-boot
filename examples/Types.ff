type Point(x: Int, y: Int)

type Bool {
    False
    True
}

type Option[T] {
    None
    Some(value: T)
}

Option_default(option: Option[T], value: T): T {
    | Some(x), _ => x
    | None, x => x
}
