type Point(x: Int, y: Int)

type Bool {
    False
    True
}

type Option[T] {
    None
    Some(value: T)
}

// sum[T: Monoid](list: List[T]) {
//     | Empty => Monoid.zero()
//     | Link(head, tail) => Monoid.add(head, sum(tail))
// }

// trait Show[T] {
//     show(value: T): String
// }
