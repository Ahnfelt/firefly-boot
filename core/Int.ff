type Int {} scala "scala.Int"

extend self: Int {
    getTo(inclusive: Int): List[Int] { panic("magic") } scala "self_.to(inclusive).toList"
    getUntil(exclusive: Int): List[Int] { panic("magic") } scala "self_.until(exclusive).toList"
    getAbs(): Int { panic("magic") } scala "self_.abs"
}
